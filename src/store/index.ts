import { createStore } from "vuex";
import modalState from "./modalState";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    modalState,
  },
});
